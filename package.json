{
  "name": "yeschef",
  "version": "0.1.0",
  "description": "YesChef — a kitchen-brigade AI harness plugin for Claude Code: token thrift, clean context, autonomous service.",
  "type": "module",
  "private": true,
  "license": "Apache-2.0",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "node build.mjs",
    "test": "vitest run",
    "check": "tsc --noEmit",
    "verify": "npm run check && npm run build && npm run test"
  },
  "devDependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "@types/node": "^18.19.0",
    "esbuild": "^0.21.5",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0",
    "zod": "^3.23.8"
  }
}
